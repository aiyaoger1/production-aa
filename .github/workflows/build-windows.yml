name: 生产下单系统构建

# 触发条件
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # 重要：允许手动运行

# 环境变量
env:
  PYTHON_VERSION: '3.9'

# 构建任务
jobs:
  build-windows-exe:
    name: 构建Windows EXE
    runs-on: windows-latest
    
    steps:
      # 步骤1：检出代码
      - name: 检出仓库代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      # 步骤2：设置Python
      - name: 设置Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          
      # 步骤3：验证文件
      - name: 验证必要文件
        run: |
          if not exist "app.py" (
            echo "错误：缺少 app.py"
            exit 1
          )
          echo "✅ 文件验证通过"
          
      # 步骤4：安装依赖
      - name: 安装依赖包
        run: |
          python -m pip install --upgrade pip
          pip install flask==2.3.0 pyinstaller==5.13.0
          
      # 步骤5：打包EXE
      - name: 生成可执行文件
        run: |
          pyinstaller ^
            --onefile ^
            --name "ProductionOrderSystem" ^
            --clean ^
            app.py
          
      # 步骤6：上传构建产物
      - name: 上传EXE文件
        uses: actions/upload-artifact@v4
        with:
          name: Production-Order-System
          path: dist/ProductionOrderSystem.exe
          retention-days: 7
          
      # 步骤7：完成通知
      - name: 构建完成
        run: |
          echo "🎉 EXE文件构建成功！" >> $GITHUB_STEP_SUMMARY
          echo "文件位置：dist/ProductionOrderSystem.exe" >> $GITHUB_STEP_SUMMARY
          echo "大小：$((Get-Item 'dist/ProductionOrderSystem.exe').Length / 1MB) MB" >> $GITHUB_STEP_SUMMARY

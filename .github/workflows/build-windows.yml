name: 构建生产下单系统EXE

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      # 步骤1：获取代码
      - name: 检出仓库代码
        uses: actions/checkout@v4
      
      # 步骤2：设置Python环境
      - name: 设置Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      
      # 步骤3：安装依赖包
      - name: 安装必要包
        run: |
          python -m pip install --upgrade pip
          pip install flask==2.3.0
          pip install pyinstaller==5.13.0
      
      # 步骤4：确保app.py存在（修复版本）
      - name: 准备主程序文件
        shell: pwsh
        run: |
          if (-not (Test-Path "app.py")) {
            Write-Host "创建app.py文件..."
            
            # 使用Here-String创建文件内容
            $appContent = @'
from flask import Flask
import os

app = Flask(__name__)

@app.route('/')
def index():
    return """<!DOCTYPE html>
<html>
<head>
    <title>生产下单系统</title>
    <meta charset="utf-8">
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .header { background: #4CAF50; color: white; padding: 20px; border-radius: 10px; }
        .feature { background: #f5f5f5; padding: 15px; margin: 10px 0; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>✅ 生产下单系统</h1>
        <p>版本 1.0 | 独立EXE程序</p>
    </div>
    
    <h2>功能模块</h2>
    <div class="feature">
        <h3>📋 订单管理</h3>
        <p>创建、查看、跟踪生产订单</p>
    </div>
    <div class="feature">
        <h3>👥 客户管理</h3>
        <p>管理客户信息和联系方式</p>
    </div>
    <div class="feature">
        <h3>📦 产品管理</h3>
        <p>维护产品目录和规格</p>
    </div>
    <div class="feature">
        <h3>🏭 生产跟踪</h3>
        <p>实时跟踪订单生产进度</p>
    </div>
    
    <p><strong>使用说明：</strong>所有数据保存在本地，无需网络连接。</p>
</body>
</html>"""

if __name__ == '__main__':
    port = int(os.environ.get('PORT', 5000))
    print(f"生产下单系统启动... 访问地址: http://localhost:{port}")
    app.run(host='0.0.0.0', port=port, debug=False)
'@
            
            Set-Content -Path "app.py" -Value $appContent -Encoding UTF8
            Write-Host "✅ app.py 已创建"
          } else {
            Write-Host "✅ app.py 已存在"
          }
      
      # 步骤5：打包EXE文件
      - name: 生成可执行文件
        run: |
          echo 开始打包EXE文件...
          pyinstaller --onefile --name "生产下单系统" --clean --noconfirm app.py
          echo 打包完成！
      
      # 步骤6：检查结果
      - name: 验证EXE文件
        shell: pwsh
        run: |
          $exePath = "dist/生产下单系统.exe"
          if (Test-Path $exePath) {
            $size = (Get-Item $exePath).Length / 1MB
            $sizeRounded = [math]::Round($size, 2)
            Write-Host "✅ EXE文件生成成功！"
            Write-Host "文件大小: ${sizeRounded} MB"
          } else {
            Write-Error "❌ EXE文件生成失败"
            exit 1
          }
      
      # 步骤7：创建启动脚本
      - name: 创建启动文件
        shell: pwsh
        run: |
          $batchContent = @'
@echo off
chcp 65001 >nul
echo ========================================
echo     生产下单系统 v1.0
echo ========================================
echo.
echo 正在启动程序...
echo 首次启动可能需要10-30秒，请耐心等待...
echo.
echo 启动成功后，请在浏览器中访问：
echo.
echo      http://localhost:5000
echo.
echo 按 Ctrl+C 可停止程序
echo ========================================
echo.
生产下单系统.exe
pause
'@
          
          Set-Content -Path "dist/启动系统.bat" -Value $batchContent -Encoding UTF8
          Write-Host "✅ 启动脚本已创建"
      
      # 步骤8：上传结果
      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: 生产下单系统
          path: |
            dist/生产下单系统.exe
            dist/启动系统.bat
